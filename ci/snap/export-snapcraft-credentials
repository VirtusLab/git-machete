
if [[ "$OSTYPE" == "darwin"* ]]; then
  wrap0_flag=""
else
  wrap0_flag="--wrap=0"
fi

cat ~/.snapcraft.login
export SNAPCRAFT_LOGIN_CREDENTIALS_CONTENTS_BASE64=$(base64 $wrap0_flag ~/.snapcraft.login)
